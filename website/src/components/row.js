import styled from '@emotion/styled';
import theme from '../theme';

const Row = styled.div`
  display: flex;
  margin-left: -${theme.space[3] / 2};
  margin-right: -${theme.space[3] / 2};
`;

export default Row;
