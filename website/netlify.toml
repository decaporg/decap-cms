[build]
  base = "website/"
  command = "yarn install && yarn build"
  publish = "dist/"

  [build.environment]
    NODE_VERSION = "12"
    YARN_FLAGS = "--frozen-lockfile"
    YARN_VERSION = "1.22.4"
