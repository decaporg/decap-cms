import React from 'react';
import { Menu as RTMenu } from 'react-toolbox/lib/menu/index';
import 'react-toolbox/lib/commons.scss';

const Menu = (props) => (
  <RTMenu {...props}/>
);

export default Menu;
