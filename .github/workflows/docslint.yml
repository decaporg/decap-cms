name: 'Docs Linting:'
on:
  push:
    branches:
      - master

    paths:
      - '.github/workflows/docslint.yml'
      - 'website/**'

  pull_request:
    types: [opened, synchronize, reopened]

    paths:
      - '.github/workflows/docslint.yml'
      - 'website/**'

jobs:
  Prose:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Vale Linter
        uses: errata-ai/vale-action@8683aa9e6370d27cefcbed7766979dcd021293ad
        with:
          # Optional
          files: website/content/docs
        env:
          # Required
          GITHUB_TOKEN: ${{secrets.GITHUB_TOKEN}}
