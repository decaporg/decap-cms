import {
  cmsMarkdownViewer
} from 'cms/helpers/cms-markdown-viewer';

module('CmsMarkdownViewerHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = cmsMarkdownViewer(42);
  ok(result);
});
