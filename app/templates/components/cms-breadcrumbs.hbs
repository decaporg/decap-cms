<div class="cms cms-breadcrumbs">
  {{#each crumb in breadcrumbs}}
    <a href="#" {{action "gotoCrumb" crumb}} class="cms cms-breadcrumb {{crumb.class}}">{{crumb.label}}</a>
    {{#unless crumb.last}}<span class="cms cms-breadcrumb-separator"> / </span>{{/unless}}
  {{/each}}
</div>
